[project]
name = "odj-db-register"
version = "0.1.0"
description = "オタクDJ大会のセットリストファイルからプレイログDBを構築する"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/odj"]
