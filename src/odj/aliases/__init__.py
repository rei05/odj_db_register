"""曲名・元ネタ・アーティストの表記ゆれを名寄せするための下ごしらえ。

最終的な「この表記たちは同じものか」の判定は LLM と人間がやる。ここは
その前段で、**決定的に**（実行するたび同じ結果になるやり方で）

  1. 規則で書けるゆれは規則で潰し（`rules`）
  2. 残りを「同じものかもしれない値の集まり」に切り分ける（`block`）

までを担当する。入力は `web/public/data/plays.json` だけで、Drive には触らない
（GitHub Actions 上でも動かすため）。
"""
